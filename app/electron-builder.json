{
  "publish": null,
  "files": ["out/**/*"],
  "appId": "com.facenox.app",
  "productName": "Facenox",
  "icon": "./public/icons/logo.png",
  "beforePack": "scripts/before-pack.mjs",
  "win": {
    "target": "nsis",
    "artifactName": "Facenox-Windows-v${version}-${arch}.${ext}",
    "extraResources": [
      {
        "from": "../server/dist/server.exe",
        "to": "server/server.exe"
      },
      {
        "from": "public/icons",
        "to": "icons"
      }
    ],
    "icon": "./public/icons/logo.ico"
  },
  "nsis": {
    "oneClick": false,
    "allowToChangeInstallationDirectory": true,
    "perMachine": false,
    "createDesktopShortcut": true,
    "createStartMenuShortcut": true,
    "deleteAppDataOnUninstall": false,
    "runAfterFinish": true,
    "artifactName": "Facenox-Windows-v${version}-${arch}.${ext}"
  },
  "linux": {
    "target": "AppImage",
    "artifactName": "Facenox-Linux-v${version}-${arch}.${ext}",
    "category": "utility",
    "extraResources": [
      {
        "from": "../server/dist/server",
        "to": "server/server"
      },
      {
        "from": "public/icons",
        "to": "icons"
      }
    ],
    "icon": "./public/icons/logo-transparent.png"
  },
  "mac": {
    "target": "dmg",
    "artifactName": "Facenox-macOS-v${version}-${arch}.${ext}",
    "entitlements": "entitlements.mac.plist",
    "entitlementsInherit": "entitlements.mac.plist",
    "extendInfo": {
      "NSCameraUsageDescription": "This app requires camera access to capture video frames for secure, real-time facial recognition and attendance tracking."
    },
    "extraResources": [
      {
        "from": "../server/dist/server",
        "to": "server/server"
      }
    ],
    "icon": "./public/icons/logo.icns"
  }
}
