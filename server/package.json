{
  "name": "facenox-server",
  "private": true,
  "scripts": {
    "dev": "python run.py",
    "build": "python build_backend.py",
    "clean": "npx rimraf build dist .ruff_cache .pytest_cache && python -c \"from pathlib import Path; import shutil; [shutil.rmtree(p) for p in Path('.').rglob('__pycache__')]; [p.unlink() for p in Path('.').rglob('*.py[co]')]\"",
    "test": "pytest",
    "lint": "../venv/bin/ruff check --fix || ./venv/bin/ruff check --fix || ruff check --fix",
    "format": "../venv/bin/black . || ./venv/bin/black . || black .",
    "fix": "npm run lint && npm run format"
  }
}
